{
  "name": "icm-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Microsoft IcM (Incident Management) - query, create, update incidents via LLMs",
  "type": "module",
  "main": "./build/index.js",
  "bin": {
    "icm-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node build/index.js"
  },
  "keywords": ["mcp", "icm", "incident-management", "microsoft"],
  "license": "MIT",
  "dependencies": {
    "@azure/identity": "^4.6.0",
    "@modelcontextprotocol/sdk": "^1.13.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
